# Kafka Configuration for Schedulord
# Topics are auto-created by KafkaJS producer
# Manual topic creation can be done via:
#   docker exec -it schedulord-kafka kafka-topics.sh --create --topic schedulord.allocation.requests --bootstrap-server localhost:9092 --partitions 3 --replication-factor 1
